name: Portfolio Terminal CI/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      - run: npm install
      - run: npm test

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.PORTFOLIO_CI }}
          publish_dir: ./build

# name: CI/CD Pipeline

# on: [push]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     permissions:
#       packages: write
      
#     steps:
#       - uses: actions/checkout@v3
      
#       - name: Verify nginx.conf exists
#         run: |
#           ls -la
#           if [ ! -f "nginx.conf" ]; then
#             echo "nginx.conf is missing!"
#             exit 1
#           fi
      
#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
      
#       - name: Build and push
#         uses: docker/build-push-action@v4
#         with:
#           push: true
#           tags: ghcr.io/manthan-sinojiya/portfolio-terminal:latest
#           file: Dockerfile